mutation createLutron($lutronType: String!, $lutronValue: String!) {
  updateIntegration(type: $lutronType, value: $lutronValue)
}

mutation createHarmony {
  updateIntegration(type: "harmony", value: "{\"areaName\":\"Family Room\"}")
}

mutation createKasa {
  updateIntegration(type: "kasa", value: "")
}

query getdevices {
  menuConfiguration {
    active
    areaName
    index
    devices {
      id
      areaName
      integration
      name
      state
      type
      value
    }
  }
}
query integrations {
    integrations {
      integrations
      harmony {
        activityId
        id
        name
      }
      hass {
        devices {
          areaName
          name
          entityId
          state
          type
          group
        }
        interfaceDevices {
          areaName
          name
          entityId
          state
          type
          group
        }
        automatedDevices {
          areaName
          name
          entityId
          state
          type
          group
        }
        token
        url
      }
      kasa {
        devices {
          areaName
          id
          ipAddress
          name
          type
        }
      }
      lutron {
        config {
          port
          url
          password
          username
          file
        }
        # inventory {
        #   areaName
        #   id
        #   name
        #   state
        #   type
        #   value
        # }
      }
    }
  }
mutation changeDeviceStatus (
  $id: String!, 
  $integration: String!, 
  $status: String!, 
  $level: String!) {
  changeDeviceStatus(
    id: $id, 
    integration: $integration, 
    status: $status, 
    level: $level)
}

# query kasaDevices {
#   kasaDevices {
#     areaName
#     id
#     ipAddress
#     name
#     on
#     type
#   }
# }

mutation assignKasaToRoom($ipAddress: String!, $areaName: String!) {
  changeAreaForKasaDevice(ipAddress: $ipAddress, area: $areaName)
}